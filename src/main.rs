use std::io;

fn dry_to_cooked(dry_bean_cups: i32):
   dry_bean_cups*2 

fn main() {
    let mut buffer = String::new();
    io::stdin().read_line(&mut buffer)?;
    let my_int = my_string.parse::<i32>().unwrap();

} 
